# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate

# Xcode — build artifacts
build/
DerivedData/

# Swift Package Manager
.swiftpm/
.build/
Package.resolved

# Node / JS toolchain (esbuild bundle pipeline — js/ 폴더)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated JS bundle (js/build.mjs → Xcode Resources)
ios/ReactNativeLightDemo/Resources/bundle.js

# Internal design docs — keep local only
docs/
